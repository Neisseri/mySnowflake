#ifndef PARAS
    #define PARAS
    #include "./total.h"
    const lowprecision dx=0.03, dy=0.03, dxdy=dx*dy;
    const lowprecision tau = 0.0003; 
    const lowprecision epsilonb = 0.01; 
    const lowprecision kappa = 1.8; 
    const lowprecision delta = 0.02;
    const lowprecision aniso = 4.0; 
    const lowprecision alpha = 0.9; 
    const lowprecision gama = 10.0; 
    const lowprecision teq = 1.0; 
    const lowprecision theta0 = 0.0; 
    const lowprecision dtime=1.0e-4;
    const int seed=10;
#endif